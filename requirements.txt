# ProtoForge Production Dependencies
# Install: pip install -r requirements.txt
# For development: pip install -r requirements-dev.txt

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
pymodbus>=3.6.0
aiosqlite>=0.19.0
websockets>=12.0
httpx>=0.27.0
PyJWT>=2.8.0
passlib[bcrypt]>=1.7.4
bcrypt<4.1
alembic>=1.13.0

# Optional protocol dependencies (install as needed)
# asyncua>=1.0.0        # OPC UA
# cryptography>=42.0.0   # OPC UA crypto
# amqtt>=0.11.0          # MQTT broker
# bacpypes>=0.18.0       # BACnet
# python-snap7>=1.3.0    # S7
# asyncpg>=0.29.0        # PostgreSQL
# grpcio>=1.62.0         # gRPC
# grpcio-tools>=1.62.0   # gRPC tools
